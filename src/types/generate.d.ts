interface GenerateOptions {
  url: string;
  theme: string;
  title?: string;
  badge?: string;
  width: number;
  height: number;
  textColor: 'auto' | 'light' | 'dark';
  compactMode: boolean;
  showGrid: boolean;
}
